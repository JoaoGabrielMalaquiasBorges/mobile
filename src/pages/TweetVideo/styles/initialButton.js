const initialButton = {
    container: {
        height: '100%',
        width: '100%',
        position: 'absolute',
        zIndex: 1,
        justifyContent: 'center',
        alignItems: 'center'
    },
    button: {
        height: 50,
        width: 50,
        borderWidth: 3,
        borderRadius: 30,
        borderColor: 'white',
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: '#1DA1F2'
    }
}

export default initialButton